
const mockEventBinding = require('./src/mockEventBinding');

module.exports = {
  /**
   * mock addEventListener/removeEventListener using jQuery 
   * so you can use jQuery to trigger event.
   */
  mockEventBinding
}